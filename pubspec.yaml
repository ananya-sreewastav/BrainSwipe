name: brainswipe
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^3.1.2
  shared_preferences: ^2.0.13
  google_fonts: ^4.0.3
  provider: ^6.0.3
  cupertino_icons: ^1.0.6
  file_picker: ^5.2.0 # Correct placement and indentation
  scratcher: ^2.5.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

